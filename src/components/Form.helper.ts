export const ColHmd = (h: number) => ({
  xs: 24,
  sm: 24,
  md: 24,
  lg: h,
  xl: h,
  xxl: h,
});

export const ColHxs = (h: number) => ({
  xs: 24,
  sm: h,
  md: h,
  lg: h,
  xl: h,
  xxl: h,
});
