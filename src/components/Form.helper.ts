export const ColH = (h: number) => ({
  xs: 24,
  sm: 24,
  md: 24,
  lg: h,
  xl: h,
  xxl: h,
});

export const ColHm = (h: number) => ({
  xs: 24,
  sm: h,
  md: h,
  lg: h,
  xl: h,
  xxl: h,
});
